
<!-- APP MAIN ==========-->
<main id="app-main" class="app-main">
    <div class="wrap">
      <section class="app-content">
          <div class="row">
              <div class="col-md-12">
                  <div class="widget">
                      <header class="widget-header">
                          <h4 class="widget-title">Delete File</h4>
                      </header><!-- .widget-header -->
                      <hr class="widget-separator">
            <%- include('partials/messages') %>
          
                      <div class="widget-body">
                <form method="post"  action="/deletefile" >
                  <div class="form-group">
                     <label for="exampleInputEmail1">Chose Bucket</label> 
                     <select class="form-control" id="exampleSelect1" onchange="selectChange(this)" name="bucketname" > 
  
                      <% buckets.forEach(user => { %>
                        <option class="form-control" value=<%= user.Name %> selected="selected"><%= user.Name %></option>
                    <% });%>
                      </select> 
                    </div> 
                    <div class="form-group"> 
                      <label for="exampleInputPassword1">Folder Name</label> 
                      <select class="form-control" id="folder" onchange="selectChange1(this)" name="folder" > 
                      </select>
                  
                     </div> 
                    <div class="form-group">
                      <label for="exampleInputPassword1">File Name</label> 
                      <select class="form-control" id="filename" name="filename" > 
                      </select>
                  
                    </div> 
                    <input class="btn btn-primary btn-block btn-lg" type="submit"  > 
                          </form>
                      </div><!-- .widget-body -->
                  </div><!-- .widget -->
              </div><!-- END column -->
          </div><!-- .row -->
      </section><!-- #dash-content -->
  </div><!-- .wrap -->
    <!-- APP FOOTER -->
    <div class="wrap p-t-0">
      <footer class="app-footer">
        <div class="clearfix">
          <div class="copyright pull-left"> &copy;2021 | All Rights Reserved | Scroll Mantra</div>
        </div>
      </footer>
    </div>
    <!-- /#app-footer -->
  </main>
  <!--========== END app main -->
  
  <script>
    let fullresult = [];    
    function selectChange(e){ 
  
      var xmlhttp = new XMLHttpRequest();
      
      // document.getElementById("folder").innerHTML = ''
  xmlhttp.onreadystatechange = function() {
    
    if (xmlhttp.readyState == 4&& xmlhttp.status == 200) {   // XMLHttpRequest.DONE == 4
    
       if (xmlhttp.status == 200) {
        //  console.log("test",xmlhttp.responseText);
  
         let result = JSON.parse(xmlhttp.responseText)
        // console.log(result)
        fullresult = result.result1;
         let result1 = [];
         for(let i=0;i<result.result.length;i++)
         result1.push(result.result[i]);
        //  console.log(result1)
        let select = document.getElementById("folder")
        if(select.options.length !=0) select.innerHTML = ''
         result1.forEach(name =>{
           
          let opt = document.createElement('option');
          opt.value = name;
          opt.innerHTML = name;
          select.appendChild(opt);
            
  
         })
          //  document.getElementById("myDiv").innerHTML = xmlhttp.responseText;
       }
       else if (xmlhttp.status == 400) {
          alert('There was an error 400');
       }
       else {
           alert('something else other than 200 was returned');
       }
    }
  };
  
  xmlhttp.open("GET", `ajaxupload2/${e.value}`, true);
  xmlhttp.send();
  
    }
    function selectChange1(e){
      console.log(e.value)
      // console.log(fullresult)
      let lastresult=[];
      fullresult.map( result => {if(result.search(e.value)!=-1) {
        lastresult.push(result.split("/").pop())
      }})
      // console.log(lastresult) 
      if(lastresult.length!=0){
      let select = document.getElementById("filename")
        if(select.options.length !=0) select.innerHTML = ''
         lastresult.forEach(name =>{
           
          let opt = document.createElement('option');
          opt.value = name;
          opt.innerHTML = name;
          select.appendChild(opt);
            
  
         })
        }
    }
  
  </script>
  